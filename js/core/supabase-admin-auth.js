/* ALBUKHR SUPABASE ADMIN AUTH CORE
 * File: js/core/supabase-admin-auth.js
 * Fix: use albukhr_security.get_my_admin_context() instead of the old get_my_admin_role().
 */
(function(window){"use strict";
const CORE_NAME="ALBUKHR Supabase Admin Auth";let initialized=false,currentAdmin=null,authSubscription=null;
function fail(m){throw new Error(m)}
function getEnvironment(){const e=window.ALBukhrEnvironment;if(!e||typeof e.isKnown!=="function")fail("ALBUKHR Environment Core is unavailable.");if(!e.isKnown()||!e.isMainnet())fail("Admin authentication is available only on ALBUKHR MAINNET.");return e}
function getSupabaseCore(){const c=window.ALBUKHR_SUPABASE;if(!c||!c.client)fail("ALBUKHR Supabase Core is unavailable.");return c}
function getClient(){getEnvironment();return getSupabaseCore().client}
async function getAdminContextFromDatabase(){const c=getClient();if(typeof c.schema!=="function")fail("Supabase client does not support schema-qualified RPC calls.");const {data,error}=await c.schema("albukhr_security").rpc("get_my_admin_context");if(error){console.error("["+CORE_NAME+"] get_my_admin_context failed:",error);throw error}return Array.isArray(data)?(data[0]||null):(data||null)}
async function init(){if(initialized)return true;getEnvironment();getClient();const {data,error}=await getClient().auth.getSession();if(error)console.error(CORE_NAME+" session check failed:",error);if(data&&data.session){try{await refreshAdminContext(data.session)}catch(e){console.error(CORE_NAME+" initial admin context check failed:",e);currentAdmin=null}}
if(!authSubscription){const {data:listener}=getClient().auth.onAuthStateChange(async function(event,session){console.info("[ALBUKHR ADMIN AUTH]",event);if(session){try{await refreshAdminContext(session)}catch(e){console.error("Admin context refresh failed:",e);currentAdmin=null}}else currentAdmin=null});authSubscription=listener&&listener.subscription?listener.subscription:null}initialized=true;console.info("✅ "+CORE_NAME+" initialized.");return true}
async function signIn(email,password){getEnvironment();if(!email||!password)fail("Email and password are required.");const c=getClient();const {data,error}=await c.auth.signInWithPassword({email:String(email).trim().toLowerCase(),password:String(password)});if(error)throw error;if(!data||!data.session||!data.user)fail("Supabase authentication returned no valid session.");const context=await refreshAdminContext(data.session);if(!context||!context.is_admin||context.active===false){await c.auth.signOut();currentAdmin=null;fail("This account is not authorized for ALBUKHR administration.")}return context}
async function refreshAdminContext(session){if(!session||!session.user){currentAdmin=null;return null}const row=await getAdminContextFromDatabase();if(!row){currentAdmin={is_admin:false,user_id:session.user.id,email:session.user.email||null,role:null,active:false};return currentAdmin}currentAdmin=Object.freeze({is_admin:Boolean(row.is_admin),user_id:session.user.id,email:session.user.email||null,role:row.role||null,active:row.active!==false,mfa_required:row.mfa_required!==false,mfa_verified:Boolean(row.mfa_verified)});return currentAdmin}
async function requireAdmin(options={}){await init();const redirect=options.redirect!==false,loginUrl=options.loginUrl||"admin-login.html";const {data,error}=await getClient().auth.getSession();if(error)throw error;if(!data||!data.session){currentAdmin=null;if(redirect)window.location.replace(loginUrl);return null}const context=await refreshAdminContext(data.session);if(!context||!context.is_admin||context.active===false){await getClient().auth.signOut();currentAdmin=null;if(redirect)window.location.replace(loginUrl);return null}return context}
async function requireRole(roles,options={}){const admin=await requireAdmin(options);if(!admin)return null;const allowed=Array.isArray(roles)?roles.map(String):[String(roles)];if(!allowed.includes(admin.role)){if(options.onDenied)options.onDenied(admin);else console.error("❌ Admin role denied:",admin.role);return null}return admin}
async function ensureMfa(){const c=getClient(),{data,error}=await c.auth.mfa.listFactors();if(error)throw error;const verifiedTotp=(data.totp||[]).find(f=>f.status==="verified");if(!verifiedTotp)return{required:false,verified:true};const assurance=await c.auth.mfa.getAuthenticatorAssuranceLevel();if(assurance.error)throw assurance.error;if(assurance.data.currentLevel==="aal2")return{required:true,verified:true};return{required:true,verified:false,factorId:verifiedTotp.id}}
async function verifyMfa(factorId,code){if(!factorId||!code)fail("MFA factor and verification code are required.");const c=getClient();const {data:challenge,error:ce}=await c.auth.mfa.challenge({factorId});if(ce)throw ce;const {data,error}=await c.auth.mfa.verify({factorId,challengeId:challenge.id,code:String(code).trim()});if(error)throw error;return data}
async function resetPassword(email,redirectTo){getEnvironment();if(!email)fail("Email is required.");const options={};if(redirectTo)options.redirectTo=redirectTo;const {error}=await getClient().auth.resetPasswordForEmail(String(email).trim().toLowerCase(),options);if(error)throw error;return true}
async function updatePassword(newPassword){if(!newPassword||String(newPassword).length<12)fail("Admin password must be at least 12 characters.");const {data,error}=await getClient().auth.updateUser({password:String(newPassword)});if(error)throw error;return data}
async function signOut(){try{await getClient().auth.signOut()}finally{currentAdmin=null}}
function getCurrentAdmin(){return currentAdmin}function isAuthenticated(){return Boolean(currentAdmin&&currentAdmin.is_admin)}function hasRole(role){return Boolean(currentAdmin&&currentAdmin.is_admin&&currentAdmin.role===String(role))}function getRole(){return currentAdmin?currentAdmin.role:null}
window.AlbukhrSupabaseAdminAuth=Object.freeze({init,signIn,requireAdmin,requireRole,refreshAdminContext,ensureMfa,verifyMfa,resetPassword,updatePassword,signOut,getCurrentAdmin,isAuthenticated,hasRole,getRole});
})(window);
